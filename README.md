# Teleagent

Telegram bot for remote control and command execution on servers.

## Features

* Secure system command execution.
* Support for interactive execution under different users (`sudo`, `$user`, `..`).
* Support for custom shortcuts (aliases) through the `config.yaml` file.
* Last-minute confirmation for destructive/critical commands.
* Historical logging with custom headers per chat.
